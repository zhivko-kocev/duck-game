apiVersion: v1
kind: Service
metadata:
  name: duck-game-backend
  namespace: duck-game
spec:
  selector:
    app: duck-game-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
